# fastapi-async

Async/ASGI variant of the same endpoint (uvicorn event loop, async HTTP client for the 3rd-party call). Compared against django-uwsgi on memory-per-concurrent-request and resilience to a slowdown injection.

Status: not yet implemented (Phase 1).
